{
  "name": "llm-diff-risk-review",
  "version": "1.0.0",
  "description": "GitHub Action: rate PR diff risk with any OpenAI-compatible LLM and auto-approve low-risk PRs.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --source-map --license licenses.txt"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0",
    "@vercel/ncc": "^0.38.1"
  }
}
